<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FDApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="button" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/dialog_add_button</item>
    </style>

    <style name="YourApplicationTheme" parent="android:style/Theme.NoTitleBar">
        <item name="android:buttonStyle">@style/button</item>
    </style>

    <style name="cardView" parent="CardView">
        <item name="android:background">@drawable/dialog_fragment_background</item>
    </style>

    <style name="BottomNavigationTheme" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:textColorPrimary">@color/selected_color</item>
        <item name="android:textColorSecondary">@color/unselected_color</item>
    </style>

    <style name="Theme.FDApp" parent="Base.Theme.FDApp" />
</resources>